# 开发环境 Dockerfile（支持热重载）

FROM node:18-alpine

# 设置工作目录
WORKDIR /app

# 安装依赖
RUN apk add --no-cache python3 make g++

# 复制 package 文件
COPY package*.json ./

# 安装所有依赖（包括 devDependencies）
RUN npm install

# 复制源代码
COPY . .

# 暴露端口
EXPOSE 3000

# 使用 nodemon 启动，支持热重载
CMD ["npm", "run", "dev"]
